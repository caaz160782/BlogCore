@{
    ViewData["Title"] = "Listado Slider";
}

<div class="conatiner mt-4">
    <div class="row">
        <div class="col-8">
            <h2 class="text-primary">Lista de articulos</h2>
        </div>
        <div class="col-4 text-right">
            <a asp-area="Admin" asp-controller="Sliders" asp-action="Create" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Crear Slider</a>

        </div>
    </div>
    <br />
    <table id="tblSliders" class="table table-striped table-bordered" style="width:100%">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre </th>
                <th>Estado </th>
                <th>Imagen</th>                
                <th>Acciones</th>
            </tr>
        </thead>
    </table>
</div>

@section Scripts {
    <script src="~/js/sliders.js"></script>
}